# HBTS 진로·학업설계 툴

HBTS 뇌 사고유형 검사 결과지 PDF를 **학생용 진로·학습 설계서**로 바꾸는 단일 HTML 도구.
주식회사 뿌리깊이 캠프 2차시의 핵심 도구.

---

## 이 저장소에서 일하는 방식

**사용자(J님)는 사업 설계자이고, 당신이 개발 전담입니다.**

- 「직접 해보세요」·「개발자가 필요할 것 같습니다」 금지
- 「이거 될까요?」 → 「됩니다, 이렇게 하면 됩니다」로 답하고 직접 처리
- 기초 설명 생략. J님은 웹 제작 중상급 (Three.js·Supabase·디자인토큰·반응형 전부 가능)
- 답변은 **짧은 문장 · 불릿 · 굵은 핵심어**. 긴 문단 금지
- 매 답변 끝에 다음 액션 아이템 하나

---

## 빌드

```bash
./build.sh          # src/ → index.html (단일 파일 약 1.5MB, pdf.js 인라인)
```

`index.html` 을 직접 고치지 마세요. **반드시 `src/` 를 고치고 빌드**합니다.

배포는 GitHub Pages (main 브랜치 루트). push 하면 1~2분 뒤 반영.

```
https://kjy62306230-cpu.github.io/HBTS_translator_students/
```

---

## 파일 구조

| 파일 | 역할 |
|---|---|
| `src/shell.html` | 레이아웃 · CSS · 입력 화면 |
| `src/kb.js` | 4영역(LAB·LPB·RAB·RPB) 특징 · 진로 지도 · 조합 |
| `src/science.js` | 학습법 4기법 · 유형별 진입로 · 시험 계획 |
| `src/roadmap.js` | 학창시절 로드맵 · 제도 정보 · 약점 대응 |
| `src/engine.js` | 점수 해석 · 설계서 렌더링 · 시각화 · AI 호출 |
| `src/upload.js` | PDF 판독 (텍스트 / 스캔본 두 경로) |
| `src/verify.js` | 기하 검산 · 소아청소년기 예시그림 판별 |

**콘텐츠만 바꿀 때는 `kb.js` / `science.js` / `roadmap.js` 세 개만 건드립니다.**

---

## ⛔ 절대 어기면 안 되는 4가지

### 1. 강의자료 p10~15 라벨은 뒤바뀌어 있습니다

원본 자료를 보고 되돌리지 마세요. 아래가 맞습니다.

```
RAB 우측전뇌 = 시각 · 공간 · 창의 · 변화
RPB 우측후뇌 = 사람 · 공감 · 화합 · 감성
```

### 2. 학습유형 이론은 연구로 지지되지 않습니다

```
무엇을 할지(WHAT)     4기법 — 유형과 무관하게 모두 같다
어떻게 시작할지(HOW)   진입로 — 유형별로 다르다
```

금지 문장: 「창의형은 그림으로 공부하면 더 잘 외운다」 「우뇌형이라 예체능이 맞다」 「뇌과학이 증명한 맞춤 학습」

⚠️ 영단어·한자는 **섞으면 오히려 해롭습니다** (g=−0.39, Brunmair & Richter 2019). 이 예외를 지우지 마세요.

### 2-1. 강의자료 p33~36 의 「유형별 학습 전략」 표는 그대로 옮기지 않습니다

검토 결과 원안에 세 가지 문제가 있어 재구성했습니다. **되돌리지 마세요.**

| 원안 | 문제 | 우리 처리 |
|---|---|---|
| 「가르쳐보기」를 우측후뇌 전용으로 배정 | 자기설명 효과(g=0.55)는 유형 무관 | **공통 도구함**으로 전부 개방 |
| 「이미지·연상 암기」를 우측전뇌 전용 | 그림 우월 효과는 보편적 | 공통 도구함 |
| 복습 간격을 유형으로 배정 (단기/장기) | 간격은 유형이 아니라 **시험까지 남은 기간**이 정한다 (Cepeda 2008) | 유형에서 분리 |
| 「교과서 암기」 | 사실상 다시 읽기 — 효과 최하위 | 삭제 |

**대신 유형별로 주는 것은 두 가지뿐입니다.**

```
FIRST_TOOLS  먼저 잡을 도구 셋 — 효과가 아니라 「손이 가느냐」로 고름
QUIT_RISK    나를 그만두게 만드는 방식 — 원안에서 가장 값어치 있는 부분
```

`QUIT_RISK` 는 **효과 주장이 아니라 지속·이탈 주장**이라 근거가 안전합니다. 이 프레이밍을 깨지 마세요.

### 2-2. 시험 계획의 시작 강도는 유형이 아니라 「공부에 대한 감정」으로 나눕니다

```
PACE.hard   공부 긍정 < 부정  → 1주차 하루 20분·3덩어리. 목표는 진도가 아니라 「7일 연속」
PACE.mid    같음             → 45분으로 시작
PACE.ok     긍정 > 부정      → 표준 계획표 그대로
SOLO        외향:내향으로 혼자/함께 형태 결정
```

동기·정서에 따라 개입을 달리하는 것은 학습유형 논란과 **무관하게 정당**합니다.

### 3. 결과지 원문·직업표를 그대로 전재하지 않습니다

참고해서 **우리 문장으로 재구성**합니다. 검사 문항을 툴에 넣지 않습니다.

### 3-1. 학생 데이터를 서버에 저장하지 않습니다 (2026-08-24 J님 확정)

이 툴은 **해석 도구**다. 학생 관리 시스템이 아니다.

```
서버에 남는 것   설계서 건수 · 캠프 코드 · AI 사용량   (개인정보 없음)
남지 않는 것     이름 · 점수 · 학생이 쓴 글 · 결과지 PDF
```

학생이 화면에 쓴 내용은 **그 기기의 브라우저에만** 저장된다(localStorage).
서버로 올라가지 않는다. 학생이 PDF로 뽑아 가면 그걸로 끝이다.

**「학급 결과 저장」·「학생별 DB」를 다시 만들자는 제안이 나오면 이 결정을 먼저 보여줄 것.**
기술이 어려워서 뺀 게 아니라(3KB/명이면 충분) **의도적으로 뺀 것**이다.

이유
- 학교 계약에서 개인정보 항목이 통째로 사라진다 — 진로부장이 가장 걱정하는 부분
- 「저희는 학생 데이터를 저장하지 않습니다」가 오히려 신뢰를 만든다
- 툴이 한 가지(해석)를 잘하는 데 집중한다

### 4. 개인정보

**검사 결과지 PDF 를 저장소에 올리지 않습니다.** `.gitignore` 로 막혀 있습니다.
학생 이름·점수가 담긴 파일이 커밋되지 않게 하세요.

---

## 지켜야 할 동작 (되돌리면 버그가 재발합니다)

| 동작 | 이유 |
|---|---|
| 한글 텍스트는 공백 제거판(`c`)에서 정규식 매칭 | pdf.js 가 한글을 글자 단위로 쪼갬 |
| 소아청소년기 페이지는 실제 데이터 존재로 필터 | 목차(INDEX)에도 같은 문구가 있음 |
| 전체 텍스트 300자 미만이면 스캔본 모드 | 인쇄→PDF 저장본은 글자가 전부 이미지 |
| 기하 검산은 **평균이 아니라 중앙값** | 이상치 하나가 평균을 끌어 엉뚱한 영역을 지목 |
| 비교 페이지 왼쪽의 진한 파란 픽셀 < 150 → 예시 그림 | 실측 1,140px vs 14px. 가짜 점수 유입 차단 |
| 스캔 이미지는 JPEG q0.85 | PNG 5.2MB → JPEG 1.4MB. 전송 실패 방지 |
| 인쇄 CSS 는 섹션이 아니라 **최소 단위**에 break-inside:avoid | 섹션 전체에 걸면 빈 페이지가 5장 생김 (24장→19장) |

---

## 캠프 모드

```
both    결합 캠프          9섹션 전부
career  진로 캠프          03·04·05(학습법 상세) 제외 → 6섹션
study   자기주도학습 캠프   06·07(진로·로드맵) 제외 → 7섹션
```

번호는 남은 섹션에 다시 매깁니다. AI 심화의 무게중심도 모드를 따라갑니다.

## 역할 분담 — 툴 / PPT / 활동지  (2026-08-24 확정)

| | 담당 | 내용 |
|---|---|---|
| **강사 PPT** | 강사 | 네 유형의 특징 · 유형별 학습법 4종(암기·복습·필기) |
| **활동지** `worksheet.html` | 학생 손글씨 | PPT 받아적기 · 내 흐름 만들기 · 4주 플랜 · 1분 발표 |
| **툴 결과지** | 자동 생성 | 점수 해석 · **1·2순위 조합 학습법** · 진로/로드맵 · AI 심화 |

- 툴은 **정교한 해석 툴** 역할만 합니다. 학생이 툴 안에 무언가를 **쓰지 않습니다.**
- 「직접 고치기 · 내가 쓴 내용 지우기 · 활동지 인쇄」는 전부 제거했습니다.
- **유형별 4종 표는 결과지에 넣지 않습니다.** 강사가 설명하고 학생이 활동지에 적습니다.
- 결과지에 남는 학습 파트 = **조합 학습법(1·2순위)** + 하루 흐름 + 외향/내향 + A/B 테스트.
- 강사용 우회 통로 없음. 관리는 **관리자 페이지(`admin.html`)로 일원화**.

### 읽기 구조 — 카드 프레임 (2026-08-24 J님 지시)

> 「요즘 학생들 글 읽는 거 별로 안 좋아해서 **frame 씌워서 확실히 분리**해서 설명해주지 않으면 제대로 안 읽을걸」

**모든 학습·진로 내용은 반드시 카드 안에 넣습니다.** 표와 문단만 늘어놓으면 안 읽습니다.

| 조각 | 무엇 |
|---|---|
| `.lc` + `lcard()` / `ccard()` / `acard()` | 밝은 섹션의 카드. 번호 + 제목 + **한 줄 요약(sub)** 헤더 |
| `.core` | dark 섹션(03)의 기법 카드. 같은 언어로 프레임을 씌움 |
| `.only` | 카드 맨 아래 **「이것만」** 바. 그 줄만 읽어도 손해가 없어야 함 |
| `.rank` | 1·2순위 배지 |

- **`.lc .lch` 안의 `<b>`는 반드시 흰색**입니다. 안 그러면 초록 배경에 묻혀 글자가 사라집니다
- **「이것만」이 본문을 그대로 반복하면 안 됩니다.** 다른 각도의 한 줄이어야 가치가 있습니다
- 카드 안의 핵심 안내를 `.src`(11.5px 흐린 회색)로 쓰지 마세요. 안 읽힙니다

### ⚠️ 인쇄 — 카드에 break-inside:avoid 금지

`.lc` / `.core` **전체**에 걸면 한 장 넘는 카드가 통째로 밀려 **반쪽 페이지**가 생깁니다.
2026-08-21 에 섹션에서 똑같은 실수를 했고, 08-24 에 카드에서 반복했습니다.
최소 단위(`.lch` `break-after:avoid`, `.only` `.rank` `.mc` `break-inside:avoid`)만 묶습니다.

현재 페이지 수 — **진로 19장 · 자기주도학습 16장 · 결합 23장**

---

## 캠프별 섹션 순서 (2026-08-24 확정)

**진로 캠프에서 학습법이 진로보다 먼저 나오면 안 됩니다.**

```
career  프로파일 → 나는 이런 사람 → 진로 방향(최대) → 로드맵 → 지금의 마음
        → 나에게 맞는 공부법(간략) → AI와 더 대화 → 설계서가 선 자리
study   프로파일 → 나는 이런 사람 → 네 가지 기법 → 내 진입로 → 시험 계획 → 지금의 마음 → 선 자리
```

`studySec()` 을 변수로 잡아두고 **push 순서만** 바꿉니다. `num()` 은 호출 시점에 번호를 매기므로 번호는 자동으로 맞습니다.

분량 목표 — **career 는 진로 파트가 최대 섹션**이어야 합니다 (현재 30%, 학습법 12%).

### 진로 파트 구성

| 카드 | 데이터 |
|---|---|
| 어떤 분야에서 힘이 나는가 | `KB[].fields` · `COMBO` |
| 어떤 방식으로 일할 때 | `CAREER_MAP` · `WORK_STYLE` |
| **못 견디는 환경** | `CANT_STAND[p.low]` — 최저 영역이 근거 |
| **좁히기 3단계** | `NARROW.steps` — 넓히기 → 3개로 → 확인 |
| **후보 검증법** | `NARROW.verify` |
| **고교 과목 선택** | `SUBJECT_PICK` — `p.grade!=='초'` 일 때만 |

---

## AI 두 갈래 — 헷갈리지 말 것

| | 무엇 | 키 | 원가 |
|---|---|---|---|
| **① AI 심화 설계** | **툴이 직접 씀.** `startDeep()` → 프록시 | 서버(`app_settings`) | 1명 **약 100원** |
| ② AI와 더 대화해보기 | 학생이 **PDF를 ChatGPT에 올려** 대화 | 학생 계정 | 0원 |

- **진로 캠프는 ①이 자동 실행됩니다** (`generate()` 끝 `if(p.mode==='career') setTimeout(startDeep,300)`).
  버튼을 눌러야만 나오면 30명 중 몇 명은 빈손으로 나갑니다. **심화 설계서가 그날의 산출물입니다.**
- ②는 4차시 활동. 프롬프트를 길게 쓰지 않고 **PDF를 통째로 올립니다.**
- `printAnon()` — 이름을 「학생」으로 바꿔 인쇄. **기본이 아니라 옵션**입니다.
  본인 계정에 본인 이름은 문제가 아니지만, 「캠프가 지시해서 30명이 올렸다」가 되면 학교 책임이 됩니다.
- ⚠️ ChatGPT 는 **만 13세 미만 가입 불가 · 13~18세 보호자 동의**. 중1이 걸립니다.
  → **①이 자동이라 ChatGPT 없이도 같은 산출물을 받습니다.** 이 답을 준비해 두세요.

## 「나는 이런 사람입니다」 — 네 영역 전부 (2026-08-24)

> 「낮은 점수 2개도 다 넣어줘. **취약한 점 장단점 등등 다 넣어줘야지**」
> 「**79점은 평균에서 가장 높은 부분**이야」 · 「그래서 **보완하는 법** 이런것도 해야지」

**순위 1→4 를 전부 카드로** 다룹니다. 상위만 보여주면 학생이 자기를 반쪽만 봅니다.

| 순위 | 카드 내용 | 데이터 |
|---|---|---|
| 1·2 | 이런 사람입니다 + **과할 때(강점의 그림자)** | `KB[].traits` `KB[].core` · `OVERUSE[]` |
| — | 두 영역이 겹치는 자리 | `COMBO[pairKey]` |
| 3·4 | 구간별 톤 + 신호 + **보완법** + **덜 겪는 것** | `lowTone()` · `WEAK_SUPPORT[].signs/fixes` · `LOW_UPSIDE[]` |

### ⚠️ 79점을 「취약」이라 부르면 안 됩니다

`LOW_TONE` 에 **75 구간**이 따로 있습니다. 이 구간을 지우지 마세요.

| 구간 | 라벨 | 톤 |
|---|---|---|
| 80+ | 기준선을 넘은 영역 | 참고만. 의식적으로 더 쓸 필요 없음 |
| **75~79** | **기준선 바로 아래** | **보완이 아니라 「연습」.** 순위가 낮을 뿐 평균으로는 높은 편 |
| 60~74 | 보통 | 이 영역이 몰리는 과제에서만 |
| 40~59 | 취약 | 노력이 아니라 도구로 |
| ~39 | 매우 취약 | 작게·자주·도구와 함께. 안 걸리게 설계 |

카드 헤더 색도 다릅니다 — `.t-soft`(취약 아님, 녹회색) / `.t-low`(실제 취약, 갈색).

### 두 가지 서술 원칙

- **높은 영역에도 단점이 있습니다.** 강점만 나열하면 학생이 자기를 못 봅니다 (`OVERUSE`)
- **낮은 영역에도 이점이 있습니다.** 단 「낮아서 좋다」로 읽히면 위로용 헛소리가 됩니다.
  **「이 방향으로는 손해를 덜 본다」** 선에서 멈춥니다 (`LOW_UPSIDE`)

---

## ⚠️ 점수 판독 — 2026-08-24 실제 사고

**LPB 34 를 84 로 오판독한 설계서가 그대로 나갔습니다.** 최저 영역이 뒤바뀌면 설계서 전체가 틀립니다.

**왜 놓쳤나** — 기하 검산 로직은 정확했습니다. 실측으로 검증했습니다.

| 영역 | 실제 | 그림 추정 | 오차 |
|---|---|---|---|
| LAB | 79 | 80 | 1% |
| RAB | 95 | 94 | 1% |
| LPB | 34 | 35 | 3% |
| RPB | 107 | 104 | 3% |

오답(LPB 84)을 넣으면 **58% 차이로 정확히 잡아냅니다.** 새어나간 이유는 로직이 아니라 **전달 방식**이었습니다.

1. 네 개 중 「가장 나쁜 하나」만 보여줘서 눈에 안 띔
2. 경고만 하고 「설계서 만들기」를 그대로 누를 수 있었음
3. 머리글은 「자동으로 다 읽었습니다」라고 안심시킴
4. 검산이 실패(`chk===null`)하면 **아무 말도 안 함** — 검증된 줄 알게 됨

**왜 34를 놓치나** — 점수가 낮으면 꼭짓점이 **뇌 그림 중앙으로 깊이 들어오고 숫자도 중앙에 붙습니다.**
사분면 바깥 모서리만 훑는 판독은 이 숫자를 통째로 놓칩니다. Vision 프롬프트에 이 경고를 넣었습니다.

**지금의 방어**

- 검산 대조표에 **네 영역 전부** 표시 (읽어온 값 / 그림이 말하는 값 / 판정)
- 불일치가 남아 있으면 `generate()` 가 **한 번 막습니다.** 사용자가 명시적으로 확인해야 통과
- 「그림이 말하는 값으로 전부 고치기」 — **오차 15% 넘는 영역만** 고칩니다.
  맞게 읽은 값까지 덮으면 79 를 81 로 바꿔 오히려 나빠집니다
- 교정 후 **재검산**합니다. 눈속임이 아니라 실제로 맞는지 봅니다
- 검산 실패 시에도 경고합니다

**절대 하지 말 것** — 검산 경고를 조용히 넘어가게 만드는 것. 점수는 이 툴의 모든 것의 입력값입니다.

---

## 진로 캠프 4차시 흐름 (2026-08-24 J님 확정 · 이 순서가 전부)

```
1~3차시  검사 → 강사 PPT 풀이 → 툴에 결과지 넣기
         → 결과지가 「방향 3개」를 못 박아 준다   ← 여기가 핵심
         → 툴이 AI 심화 설계서를 자동 생성

4차시    설계서 PDF 를 ChatGPT 에 올린다
         → ① 직업 10개 받기 → ② 학생이 직접 3개 고르기
         → ③ 하루 일과 비교 → ④ 경로 → ⑤ 로드맵 표 받기 ★
         → ⑥ 이번 달 할 것 → ⑦ 안 맞을 이유
         → ⑤를 활동지 3장에 옮겨 그린다  = 오늘의 산출물
```

> 「4차시 안에 다 가능하게 하려면 **4차시 전에 우리 툴에서 그 학생에게 맞을 진로의
>   어느정도 확실한 방향성을 잡아줘야 해**」

**결과지를 「탐색 지도」로 두면 안 됩니다.** 그러면 4차시에 30명이 다 헤맵니다.
`DIRECTION` 이 6조합 × 3방향 = **18개 방향**을 갖고 있고, 학생은 셋 중 하나를 고르기만 합니다.

| 데이터 | 무엇 |
|---|---|
| `DIRECTION[pairKey].dirs[]` | name · why · jobs · ask(AI 질문) · signal(맞는 신호) · people(0~2) · alsoNeeds[] |
| `DIR_CAUTION[low]` | 최저 영역을 요구하는 방향에 붙는 경고 |
| `ROADMAP_DRAW` | 4차시에 그릴 다섯 칸 뼈대 |

**순서 규칙** — ① 최저 영역(`alsoNeeds` 포함 · 70점 미만)을 요구하는 방향은 **뒤로** ② 그 안에서 외향은 `people` 높은 순, 내향은 낮은 순.
⚠️ **이름에서 키워드를 추측하지 말 것.** 「조직을 굴리는 자리」의 '조직'이 사람으로 오탐돼 회계·법무 방향에 감정 경고가 붙었습니다. `people` / `alsoNeeds` 는 데이터에 명시돼 있습니다.

---

## 활동지 — 캠프별 2종

| 파일 | 캠프 | 내용 |
|---|---|---|
| `worksheet.html` | — | 고르는 표지 페이지 |
| `worksheet-career.html` | 진로 4차시 | 방향 확인 → 직업 10개 → 3개 고르기 → **로드맵 그리기** → 발표 |
| `worksheet-study.html` | 자기주도학습 4차시 | 점수 → 유형별 학습법 받아적기 → 공부 흐름 → 4주 플랜 |

- 6차시 결합 캠프는 두 장을 함께 씁니다
- PDF 는 `docs/` 에 (`.gitignore` 에 `!docs/*.pdf` 예외가 있습니다)
- **손으로 쓰는 표에는 세로 구분선이 필요합니다** (`td+td{border-left}`). 없으면 칸 경계를 모릅니다

### 활동지 인쇄 규칙
각 활동지는 **A4 정확히 4장**입니다. `.pg` 에 `padding` 을 주면 5장으로 넘어갑니다.
한 장의 콘텐츠 높이 상한은 **1009px** (267mm). 내용을 늘릴 때 반드시 재보세요.

---

## 사업 맥락 (판단이 필요할 때 참고)

- 구매자는 **학교 진로부장 선생님 · 교육청**. 학부모가 아닙니다
- 진로부장이 사는 이유 = **업무 경감 + 보고 자료**. 감동이 아니라 산출물
- 상품명에 **정책 이름(고교학점제)을 넣지 않습니다.** 정책은 바뀝니다 → 「진로학업설계」
- ⚠️ **외부 기관 프로그램 참여 사실은 학생부 기재 금지.** 「학생부에 써준다」고 하면 계약이 날아갑니다
