# HBTS 진로·학업설계 툴

HBTS 뇌 사고유형 검사 결과지 PDF를 **학생용 진로·학습 설계서**로 바꾸는 단일 HTML 도구.
주식회사 뿌리깊이 캠프 2차시의 핵심 도구.

---

## 이 저장소에서 일하는 방식

**사용자(J님)는 사업 설계자이고, 당신이 개발 전담입니다.**

- 「직접 해보세요」·「개발자가 필요할 것 같습니다」 금지
- 「이거 될까요?」 → 「됩니다, 이렇게 하면 됩니다」로 답하고 직접 처리
- 기초 설명 생략. J님은 웹 제작 중상급 (Three.js·Supabase·디자인토큰·반응형 전부 가능)
- 답변은 **짧은 문장 · 불릿 · 굵은 핵심어**. 긴 문단 금지
- 매 답변 끝에 다음 액션 아이템 하나

---

## 빌드

```bash
./build.sh          # src/ → index.html (단일 파일 약 1.5MB, pdf.js 인라인)
```

`index.html` 을 직접 고치지 마세요. **반드시 `src/` 를 고치고 빌드**합니다.

배포는 GitHub Pages (main 브랜치 루트). push 하면 1~2분 뒤 반영.

```
https://kjy62306230-cpu.github.io/HBTS_translator_students/
```

---

## 파일 구조

| 파일 | 역할 |
|---|---|
| `src/shell.html` | 레이아웃 · CSS · 입력 화면 |
| `src/kb.js` | 4영역(LAB·LPB·RAB·RPB) 특징 · 진로 지도 · 조합 |
| `src/science.js` | 학습법 4기법 · 유형별 진입로 · 시험 계획 |
| `src/roadmap.js` | 학창시절 로드맵 · 제도 정보 · 약점 대응 |
| `src/engine.js` | 점수 해석 · 설계서 렌더링 · 시각화 · AI 호출 |
| `src/upload.js` | PDF 판독 (텍스트 / 스캔본 두 경로) |
| `src/verify.js` | 기하 검산 · 소아청소년기 예시그림 판별 |

**콘텐츠만 바꿀 때는 `kb.js` / `science.js` / `roadmap.js` 세 개만 건드립니다.**

---

## ⛔ 절대 어기면 안 되는 4가지

### 1. 강의자료 p10~15 라벨은 뒤바뀌어 있습니다

원본 자료를 보고 되돌리지 마세요. 아래가 맞습니다.

```
RAB 우측전뇌 = 시각 · 공간 · 창의 · 변화
RPB 우측후뇌 = 사람 · 공감 · 화합 · 감성
```

### 2. 학습유형 이론은 연구로 지지되지 않습니다

```
무엇을 할지(WHAT)     4기법 — 유형과 무관하게 모두 같다
어떻게 시작할지(HOW)   진입로 — 유형별로 다르다
```

금지 문장: 「창의형은 그림으로 공부하면 더 잘 외운다」 「우뇌형이라 예체능이 맞다」 「뇌과학이 증명한 맞춤 학습」

⚠️ 영단어·한자는 **섞으면 오히려 해롭습니다** (g=−0.39, Brunmair & Richter 2019). 이 예외를 지우지 마세요.

### 2-1. 강의자료 p33~36 의 「유형별 학습 전략」 표는 그대로 옮기지 않습니다

검토 결과 원안에 세 가지 문제가 있어 재구성했습니다. **되돌리지 마세요.**

| 원안 | 문제 | 우리 처리 |
|---|---|---|
| 「가르쳐보기」를 우측후뇌 전용으로 배정 | 자기설명 효과(g=0.55)는 유형 무관 | **공통 도구함**으로 전부 개방 |
| 「이미지·연상 암기」를 우측전뇌 전용 | 그림 우월 효과는 보편적 | 공통 도구함 |
| 복습 간격을 유형으로 배정 (단기/장기) | 간격은 유형이 아니라 **시험까지 남은 기간**이 정한다 (Cepeda 2008) | 유형에서 분리 |
| 「교과서 암기」 | 사실상 다시 읽기 — 효과 최하위 | 삭제 |

**대신 유형별로 주는 것은 두 가지뿐입니다.**

```
FIRST_TOOLS  먼저 잡을 도구 셋 — 효과가 아니라 「손이 가느냐」로 고름
QUIT_RISK    나를 그만두게 만드는 방식 — 원안에서 가장 값어치 있는 부분
```

`QUIT_RISK` 는 **효과 주장이 아니라 지속·이탈 주장**이라 근거가 안전합니다. 이 프레이밍을 깨지 마세요.

### 2-2. 시험 계획의 시작 강도는 유형이 아니라 「공부에 대한 감정」으로 나눕니다

```
PACE.hard   공부 긍정 < 부정  → 1주차 하루 20분·3덩어리. 목표는 진도가 아니라 「7일 연속」
PACE.mid    같음             → 45분으로 시작
PACE.ok     긍정 > 부정      → 표준 계획표 그대로
SOLO        외향:내향으로 혼자/함께 형태 결정
```

동기·정서에 따라 개입을 달리하는 것은 학습유형 논란과 **무관하게 정당**합니다.

### 3. 결과지 원문·직업표를 그대로 전재하지 않습니다

참고해서 **우리 문장으로 재구성**합니다. 검사 문항을 툴에 넣지 않습니다.

### 4. 개인정보

**검사 결과지 PDF 를 저장소에 올리지 않습니다.** `.gitignore` 로 막혀 있습니다.
학생 이름·점수가 담긴 파일이 커밋되지 않게 하세요.

---

## 지켜야 할 동작 (되돌리면 버그가 재발합니다)

| 동작 | 이유 |
|---|---|
| 한글 텍스트는 공백 제거판(`c`)에서 정규식 매칭 | pdf.js 가 한글을 글자 단위로 쪼갬 |
| 소아청소년기 페이지는 실제 데이터 존재로 필터 | 목차(INDEX)에도 같은 문구가 있음 |
| 전체 텍스트 300자 미만이면 스캔본 모드 | 인쇄→PDF 저장본은 글자가 전부 이미지 |
| 기하 검산은 **평균이 아니라 중앙값** | 이상치 하나가 평균을 끌어 엉뚱한 영역을 지목 |
| 비교 페이지 왼쪽의 진한 파란 픽셀 < 150 → 예시 그림 | 실측 1,140px vs 14px. 가짜 점수 유입 차단 |
| 스캔 이미지는 JPEG q0.85 | PNG 5.2MB → JPEG 1.4MB. 전송 실패 방지 |
| 인쇄 CSS 는 섹션이 아니라 **최소 단위**에 break-inside:avoid | 섹션 전체에 걸면 빈 페이지가 5장 생김 (24장→19장) |

---

## 캠프 모드

```
both    결합 캠프          9섹션 전부
career  진로 캠프          03·04·05(학습법 상세) 제외 → 6섹션
study   자기주도학습 캠프   06·07(진로·로드맵) 제외 → 7섹션
```

번호는 남은 섹션에 다시 매깁니다. AI 심화의 무게중심도 모드를 따라갑니다.

## 강사 모드

`body.admin` 클래스로 제어. 하단 「강사용 도구」 또는 `#teacher`.
학생 화면에서는 **예시값 버튼 · API키 설정 · 교사용 페이지**가 나오지 않습니다.

---

## 사업 맥락 (판단이 필요할 때 참고)

- 구매자는 **학교 진로부장 선생님 · 교육청**. 학부모가 아닙니다
- 진로부장이 사는 이유 = **업무 경감 + 보고 자료**. 감동이 아니라 산출물
- 상품명에 **정책 이름(고교학점제)을 넣지 않습니다.** 정책은 바뀝니다 → 「진로학업설계」
- ⚠️ **외부 기관 프로그램 참여 사실은 학생부 기재 금지.** 「학생부에 써준다」고 하면 계약이 날아갑니다
